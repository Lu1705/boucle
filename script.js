"use strict";

let e=7; // nous créons une variable qui est égale à un nombre entier

for(let i=0;i<e;i++){ // création d'une boucle : la valeur de départ de i est 0 ; i est inférieur à e (pour pas que la boucle soit infinie); et i augmente de 1 à chaque fois
    console.log("nous sommes arrivé à 7");
}